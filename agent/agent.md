请帮我为当前代码仓库生成一个 `AGENTS.md` 文件（放在仓库根目录），用于向后续接入的AI助手（如Claude、Cursor、Kimi等）快速说明项目整体架构和开发规范，避免AI每次都需要重新扫描全部文件才能理解项目。

要求：
1. 先用中文撰写，语言专业、简洁，适合作为"项目速查手册"。
2. 基于当前仓库的实际文件结构和代码内容进行分析，不要虚构。
3. 文件结构建议包含以下章节（可根据实际项目删减）：
   - **项目概述**：一句话描述项目目标，面向的用户/场景。
   - **技术栈**：编程语言、核心框架、关键依赖及版本。
   - **架构总览**：模块划分、目录结构说明、数据流/调用链。
   - **核心入口**：主程序入口、CLI命令、关键配置文件位置。
   - **开发规范**：命名约定、代码风格、错误处理模式、提交规范。
   - **构建与测试**：如何安装依赖、运行测试、打包发布。
   - **AI协作提示**：本项目特有的上下文（如关键算法逻辑、领域术语、容易踩的坑）。
   - **待办/已知问题**：当前进行中的功能、未解决的bug或技术债务（可选）。
4. 如果仓库较大，请在"架构总览"中说明哪些子目录有独立的业务逻辑，方便后续在子目录下补充子级 AGENTS.md。
5. 如果检测到项目使用Python，请特别说明：虚拟环境路径、requirements/pyproject配置、是否使用特定类型注解规范。
6. 如果项目涉及数据分析/生物信息/基因组学等计算密集型任务，请说明：数据输入输出格式、中间文件存放规范、是否需要特定硬件/环境。
7. 最后，在文件末尾加一段"如何使用本文件"的说明，告诉AI阅读时应优先关注哪些章节。

请直接输出完整的 `AGENTS.md` 文件内容，使用 Markdown 格式，代码块标注语言类型。我会基于你写的内容进行修改，所以请尽量准确、详细。